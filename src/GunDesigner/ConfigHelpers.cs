using LBoLEntitySideloader.ReflectionHelpers;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace GunDesigner
{
    public class ConfigHelpers
    {
       /* private static ConfigHelpers _instance;

        public static ConfigHelpers Instance
        { get { if (_instance == null) _instance = new ConfigHelpers(); return _instance; } }


        static public void UpdateConfigEntry(object config)
        {
            var ct = config?.GetType();
            if (!ConfigReflection.AllConfigTypes().Contains(config.GetType()))
            {
                Log.log.LogError($"{nameof(UpdateConfigEntry)}: {config.GetType()} is not a config data type");
                return;
            }


            ConfigReflection.GetIdField(ct);
            ConfigReflection.get

            ConfigReflection.GetArrayField(ct);
        }*/
    }
}
